[exemptions]
libhimmelblau = "External core dependency for Azure Entra ID integration"
console-subscriber = "Optional debugging/tracing tool (console feature only)"

[shallow-exemptions]
tokio = "Core async runtime, widely used despite few direct imports"
serde = "Core serialization, used through derive macros"
async-trait = "Procedural macro for async traits, single-symbol by design"
lazy_static = "Procedural macro for static initialization, single-symbol by design"
arbitrary = "Fuzzing framework dependency (fuzz targets only)"
libfuzzer-sys = "Fuzzing framework dependency (fuzz targets only)"
uzers = "Used via fully-qualified paths (uzers::switch), detection limitation"
qrcodegen = "Used via fully-qualified paths (qrcodegen::QrCode), detection limitation"
hostname = "Used via fully-qualified paths (hostname::get), detection limitation"
urlencoding = "Used via fully-qualified paths, detection limitation"
csv = "Function-scoped imports not detected by current regex"
authenticator = "Hardware authenticator support, minimal surface area by design"
dbus = "D-Bus integration, used through identity_dbus_broker"
kanidm_lib_file_permissions = "Kanidm library dependency"
tokio-util = "Tokio ecosystem utility, minimal codec usage by design"
sd-notify = "Systemd integration library, minimal surface area"
semver = "Standard semver library, single-type usage is normal"
url = "Standard URL library, single-type usage is normal"
uuid = "Standard UUID library, single-type usage is normal"
sha2 = "Cryptographic hash library, minimal usage by design"
futures = "Core async library, extension traits"
bytes = "Core networking types library"
broker-client = "Internal workspace crate, architectural component"
identity_dbus_broker = "Internal workspace crate, architectural component"
himmelblau_policies = "Internal workspace crate, architectural component"
libkrimes = "Internal workspace crate, Kerberos protocol library"
sketching = "Tracing utility library"
openidconnect = "OIDC client library, module-based usage"
tempfile = "Standard temporary file library"
secrecy = "Security library for secret handling"
walkdir = "Standard directory traversal library"
pinentry = "GPG pinentry library for password input"
rpassword = "Password reading library"
os-release = "OS release information library"
lru = "LRU cache implementation"
configparser = "INI config parser"
notify-debouncer-full = "File system notification library"
memfd = "Memory file descriptor library"
